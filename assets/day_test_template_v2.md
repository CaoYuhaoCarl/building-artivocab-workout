## 热点时文

[在这里粘贴原文]

---
## 核心词汇

```task1_prompt
Task 1：提取最影响理解文本核心单词（人名、地名等专有词不要提取），共计20个，难度不能超过B1。务必先读取并严格遵循 [assets/group_format_template.md](group_format_template.md) 的排版格式要求，再将结果输出到下方空处👇🏻：
- 对于词族明显、复用率高、课堂上值得顺带讲解的词，通常应补一行简洁的 `派生词:`；不要把它当成极少出现的可选项。
- `派生词:` 只保留 1–3 个高价值 family members，重在帮助学生看见词性变化、常见混淆和迁移价值。
```

[Task 1输出结果处]

---
```task2_prompt
Task 2：在原文中 用 ==待替换的单词== 替换上面Group中所有单词，，然后将其输出到下方空处：
```

[Task 2输出结果处]

---

```task3_prompt
Task 3：从考核阅读理解、单词拼写、语法语境、文中的位置等角度，从[Group]单词表中选择10个单词，然后将其输出到下方空处：
- 所选的单词在文中的相隔位置不能过于集中，也不能过于分散，即不能同一个句子选择2个或多个词，且应尽量均匀地分布在文章的各个段落中；
- 同一个句子只能选择一个词；
- 必须是Task1中提取的核心词汇；
- 语法：尽可能保证所选单词涵盖不同的词性，以增加考核的维度；
- 语境：影响理解的核心词
- 拼写：高频易错的拼写、派生词(语法派生、屈折派生)
- 边界说明：Task 3 可以把派生/屈折变化当作“筛选线索”，但不要把它和 Task 1 是否展示 `派生词:` 混为一谈；Task 1 的 `派生词:` 展示应按词族价值与教学价值决定。
```

[Task 3输出结果处]

---
## 综合训练
```task4_prompt
Task 4: 将最上面的原文中的上面10个单词 用 `1________`, `2________` 替换，然后将其输出到下方空处：
- 重点：同一个词有且只能被替换一次，具体替换哪个，根据位置、语境综合判断；
- 务必确保 1________ 用 `` 包裹
```

[Task 4输出结果处]

---
```task5_prompt
Task 5: 在最初的原文中 用 ==待替换的单词== 替换上面二次筛选出来的10个单词，然后将其输出到下方空处：
```

[Task 5输出结果处]

---
## 输出训练

```task6_prompt
Task 6: 根据文章内容和上面选出的10个目标词，完成以下四项输出训练任务，输出到下方空处：
6A（3词复述）：用至少3个目标词，用2–3句英文复述这篇新闻的核心内容。
6B（5句摘要）：用恰好5句英文句子概括事件经过。
6C（因果链）：使用 because / therefore / as a result 等连接词，解释事件的因果逻辑。
6D（角色视角）：从文章相关角色的角度出发（由老师根据文章内容设定角色），用英文回应：如果你是……，你最担心/最关注的是什么？
```

[Task 6输出结果处]

---
## 高阶训练

```task7_prompt
Task 7: 完成以下两项高阶训练，输出到下方空处：
7A（写作-高阶训练）：从文章中选3个含目标词汇或语法亮点的句子，每句上方留空白行供学生写英文，下方给出中文译句；如有助于理解而不泄露答案，可在括号内保留关键词提示。
7B（阅读-高阶训练）：从文章中选1个可通过上下文推断含义的词语或表达，设问让学生猜测其含义，下方留空白行作答。
```

[Task 7输出结果处]
